# 기본사항
## 행렬
* 행과 열로 구성되는 사각형 형태로 수를 배열한 것
* 컴퓨터 분야의 활용
  * 프로그래밍언어
  * 자료구조
  * 수치해석
  * 패턴인식
  * 컴퓨터그래픽스
  * 로봇동작
* m, n이 양의 정수일때 m개의 행과 n개의 열로 구성된 직사각형의 수 배열 A를 mn행렬이라한다

## 영행렬(zero matrix)
* 모든 원소가 0인 행렬

# 행렬의 연산
## 기본연산
### 행렬의 합, 차, 스칼라 곱
* 같은 위치의 A와 B의 원소를 더해서 구해지는 행렬
* 같은 위치의 A의 원소로부터 B의 원소를 빼서 구해지는 행렬
* A의 각 원소에 k를 곱해서 구해지는 행렬

### 행렬의 합과 스칼라 곱의 연산법칙
* 행렬의 합과 스칼라 곱은 같은 크기의 행렬 A,B,C에 대해 다음과 같은 연산법칙들을 만족한다
  * a, b는 실수이고 0은 모든 원소가 0인 영행렬을 의미
  * 교환법칙(위치를 바꿈)
  * 결합법칙(순서를 바꿈)
  * 스칼라 곱의 분배법칙

### 행렬의 곱
* A가 mn행렬이고 B가 nl행렬일때 행렬의 곱 AB는 (i,j)원소가 다음과 같이 정의되는 ml행렬이다
  * A의 열과 B의 행수가 같아야함

### 행렬 곱의 연산법칙
* 행렬 A, B, C가 각 연산에 적합한 크기의 행렬이라 할때 다음과 같은 연산법칙들을 만족한다
  * 곱의 결합법칙
  * 곱의 분배법칙

## 가우스 소거법
```
x + 2y - z = 2
2x + 5y + z = 3
3x + 4y - 4z = 13

계수행렬
{1 2 -1
 2 5 1
 3 4 -4}

미지수행렬
{x
 y
 z}

상수행렬
{2
 3
 13}
```

### 기본행연산
* 행 교환 연산(Ri,j)
  * 두 행의 위치를 서로 바꾸는 연산

* 행 스케일링 연산(Ri(c))
  * 하나의 행에 0이 아닌 스칼라를 곱하는 연산

* 행 대체 연산(Ri,j(c))
  * 하나의 행에 스칼라 곱을해서 다른행에 더하는 연산

### 일차연립방정식과 행렬의 관계
* L1, L2 : 일차연립방정식
* A : L1의 확장행렬
* B : L2의 확장행렬

### 가우스 소거법(가우스 조르단 소거법)
* 가우스 소거법 :  일차연립방정식 -> 확장행렬 -> 행사다리꼴행렬
* 가우스 조르단 소거법 : -> 기약 행사다리꼴 행렬

### 행사다리꼴
* 아래조건을 만족하는 행렬을 행사다리꼴이라고 한다
  * 영행이 아닌 행은 영행의 위에 있다
  * 영행이 아닌 행의 첫번째 0이 아닌 원소를 선도원소라 하는데 모든 선도원소는 1이다
  * 주어진 행의 선도원소는 그 아래행의 선도원소보다 왼쪽에 있다

### 기약 행사다리꼴
* 행사다리꼴조건에서 아래조건을 만족하는 행사다리꼴을 기약 행사다리꼴이라고 한다
  * 선도원소가 포함된 열에서 선도원소를 제외한 모든 원소는 0이다

## 가우스 소거법 예제
```
1 0 -3 -2
0 1  5  2
2 2  9  1
```
->
```
1 0 -3 -2
0 1  5  2
0 2 15  5
```
->
```
1 0 -3 -2
0 1  5  2
0 0  5  1
```
->행사다리꼴
```
1 0 -3 -2
0 1  5  2
0 0  1 0.2
```
-> 기약 행사다리꼴
```
1 0 0 -1.4
0 1 0  1
0 0 1 0.2
```

# 행렬의 종류
## 정방행렬(square matrix)
* nn행렬을 n차 정방행렬이라고하며, n을 정방행렬의 차수라 함

## 단위행렬(unit matrix)
* n차 정방행렬에서 대각원소가 모두 1이고 나머지 원소는 모두 0인 행렬을 단위행렬이라 함

## 대각행렬(diagonal matrix)
* n차 정방행렬에서 대각원소 이외의 모든 원소가 0인 행렬을 대각행렬이라 한다
  * 대각원소가 서로 다르거나 0일수도있다

## 대칭행렬(symmetric matrix)
* n차 정방행렬에서 aij = aji인 행렬을 대칭행렬이라 한다

## 교대행렬(skew matrix)
* n차 정방행렬에서 aij = -aij이고, 대각원소가 모두 0인 행렬을 교대행렬이라 한다

## 삼각행렬(triangle matrix)
* n차 정방행렬에서
  * 상삼각행렬 : 주대각선 아래에 있는 모든 원소들이 0일 경우
  * 하삼각행렬 : 주대각선 위에 있는 모든 원소들이 0일 경우

## 전치행렬(transpose matrix)
* mn행렬 A가 주어졌을때, A의 행과 열을 서로 교환한 행렬을 A의 전치행렬, 크기는 nm이 된다

# 부울행렬
* 행렬의 모든 원소가 부울값으로만 구성된 행렬

## 부울행렬의 합, 교차, 부울곱(join, meet, boolean product)
* 크기가 mn인 두 행렬 A = [aij]와 B = [bij]가 부울행렬일때
  * A와 B의 합(join)은 (i,j) 원소가 aij v bij (or) 이고 크기가 mn인 부울행렬 C이며 C v B 로 나타낸다

  * 교차는 C = A ^ B로 나타낸다(and)

* mn행렬 A와 nl행렬 B가 부울행렬일때 부울곱은 ml부울행렬 C이다
